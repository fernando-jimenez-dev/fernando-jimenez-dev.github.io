---
import background from "../assets/background.svg";
---

<div id="container" style={`--bg-image: url('${background.src}')`}>
	<main>
		<section>
			<h1>Fernando Jim&eacute;nez</h1>
			<p class="tagline">
				<b>Staff-Level .NET Engineer | Practical Architect</b>
			</p>
			<p class="intro">
				I build reliable .NET systems, thoughtful architecture, and pragmatic
				automation.
			</p>
			<ul class="links">
				<li>
					<a
						class="font-mono"
						target="_blank"
						href="https://github.com/fernando-jimenez-dev">GitHub</a
					>
				</li>
				<li>
					<a
						class="font-mono"
						target="_blank"
						href="https://www.linkedin.com/in/fernando-jimenez-dev/">LinkedIn</a
					>
				</li>
				<li>
					<a
						class="font-mono"
						target="_blank"
						href="mailto:fernando-jimenez-dev@gmail.com"
						>Email
					</a>
				</li>
				<li>
					<a class="font-mono">fernando.jimenez.dev@gmail.com</a>
				</li>
			</ul>
		</section>
	</main>
</div>

<style>
	#container::before {
		content: "";
		position: fixed;
		inset: -10vh -10vw;
		width: 120vw;
		height: 120vh;
		background-image: var(--bg-image);
		background-size: cover;
		background-position: center;
		background-repeat: no-repeat;
		opacity: 0.32;
		pointer-events: none;
		z-index: 0;
	}

	#container {
		position: relative;
		min-height: 100vh;
		box-sizing: border-box;
		padding: 1.75rem 1.25rem;
		isolation: isolate;
	}

	main {
		position: relative;
		z-index: 1;
		min-height: calc(100vh - 3.5rem);
		display: flex;
		justify-content: center;
		align-items: center;
	}

	section {
		width: 100%;
		max-width: 48rem;
	}

	h1 {
		margin: 0 0 0.25rem;
		font-size: clamp(2rem, 5vw, 3rem);
		color: #e5e7eb;
		line-height: 1.2;
	}

	.tagline {
		margin: 0.65rem 0 0;
		font-size: 1.1rem;
		color: #9ca3af;
	}

	.intro {
		margin-top: 1.25rem;
		max-width: 40rem;
		font-size: 1.05rem;
		line-height: 1.7;
		color: #9ca3af;
	}

	.links {
		display: flex;
		flex-wrap: wrap;
		gap: 1.25rem;
		list-style: none;
		margin: 2rem 0 0;
		padding: 0;
	}

	.links a {
		color: #10b981;
		text-decoration: none;
		font-weight: 600;
	}

	.links a:hover {
		color: #10b981;
		text-decoration: underline;
		text-underline-offset: 3px;
	}

	.links a:focus-visible {
		outline: 2px solid #10b981;
		outline-offset: 2px;
	}

	@media (max-width: 768px) {
		main {
			align-items: flex-start;
		}
	}
</style>
